name: Test Workflow

on:
  push:
    branches: [main]

jobs:
  step1:
    runs-on: ubuntu-latest
    env:
      ARR1: ${{ '["auth","graphql","note","notification","sticky","survey"]' }}
    steps:
      - run: echo ${{ env.ARR1 }}
      - run: echo $ARR1
      - run: echo ${{ fromJSON('["auth","graphql","note","notification","sticky","survey"]') }}
      - run: echo ${{ fromJSON(env.ARR1) }}
